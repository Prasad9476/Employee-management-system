# Base URL of the API. Keep at "/api" so the request goes to the same origin
# (nginx proxies /api to the server). For local dev, Vite proxies /api to
# http://localhost:4000 (see vite.config.ts).
VITE_API_URL=/api