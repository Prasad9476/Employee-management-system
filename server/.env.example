DATABASE_URL=postgresql://ems_user:secret@localhost:5432/ems_db
JWT_SECRET=your-super-secret-key
PORT=4000
CLIENT_URL=http://localhost:5173
DEFAULT_TENANT_NAME=Default Company
# Secure-cookie override. Defaults to true when NODE_ENV=production; set to
# "false" when serving over plain HTTP (e.g. local docker-compose) and "true"
# when behind HTTPS.
#COOKIE_SECURE=false
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
RESEND_API_KEY=
EMAIL_FROM=EMS Pro <onboarding@resend.dev>
